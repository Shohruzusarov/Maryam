const form=document.querySelector('#login-form'),error=document.querySelector('#login-error');
fetch('/api/store').then(r=>r.json()).then(data=>{const url=String(data.settings?.logoUrl||'');if(!/^(https?:\/\/|\/uploads\/)/i.test(url))return;document.querySelectorAll('.login-brand>div,.mobile-logo').forEach(el=>{el.classList.add('full-login-logo');el.innerHTML=`<img style="display:block;max-width:240px;width:100%;height:80px;object-fit:contain;object-position:left center" src="${url}" alt="${data.settings.bakeryName||'Maryam Bakery'}">`})}).catch(()=>{});
document.querySelector('#show-password').onclick=()=>{const input=form.elements.password;input.type=input.type==='password'?'text':'password'};
form.onsubmit=async e=>{e.preventDefault();error.classList.remove('show');const button=e.submitter;button.disabled=true;button.textContent='Проверяем…';const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const result=await response.json();button.disabled=false;button.textContent='Войти →';if(!response.ok){error.textContent=result.error||'Не удалось войти';return error.classList.add('show')}location.href='/admin'};
fetch('/api/auth/me').then(r=>{if(r.ok)location.href='/admin'});
